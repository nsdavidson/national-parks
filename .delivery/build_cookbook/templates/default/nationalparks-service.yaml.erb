kind: Service
apiVersion: v1
metadata:
  name: nationalparks-<%= @environment %>
  labels:
    app: nationalparks
    env: <%= @environment %>
spec:
  selector:
    app: nationalparks
    env: <%= @environment %>
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080